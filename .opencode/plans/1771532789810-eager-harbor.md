# Plan: Add Brand Icons to Footer

## Goal
Add brand icons (Kebo, Crafter Station, Moraleja) and a separate GitHub icon to the karaoke0 app footer, matching the same elements found on sql4all.org.

## What sql4all.org has
In its footer, sql4all.org displays brand icons as links with SVG logos separated by vertical dividers (`h-4 w-px bg-border`):
1. **Crafter Station** - links to `https://www.crafterstation.com`
2. **Moraleja Design** - links to `https://www.moraleja.co`
3. **Kebo** - links to `https://kebo.app`
4. **GitHub** (separate) - links to the repo

## Changes

### File: `src/routes/index.tsx`
Modify the existing `<motion.footer>` section (lines 128-135) to:

1. Keep the "Built with Suno + ElevenLabs" text
2. Add a row of brand icon links below, matching sql4all's pattern:
   - Crafter Station SVG icon -> `https://www.crafterstation.com`
   - Vertical divider
   - Moraleja SVG icon -> `https://www.moraleja.co`
   - Vertical divider
   - Kebo SVG icon -> `https://kebo.app`
   - Vertical divider
   - GitHub SVG icon -> `https://github.com/crafter-station/karaoke0`

3. Style: Icons rendered as `currentColor` using zinc-500/zinc-600 tones with `hover:opacity-80 transition-opacity`, sized at `h-5 w-auto` (matching sql4all). The icons section uses `flex items-center gap-3` layout with `w-px bg-zinc-700` dividers adapted for the dark theme.

### SVGs Used (inline, same as sql4all)
- **Crafter Station**: The infinity-knot logo SVG (257x257 viewBox)
- **Moraleja**: The star/compass logo SVG (184.3x184.3 viewBox) — note: the inner face details use `fill-background` class which we'll change to `fill-zinc-950` for our dark background
- **Kebo**: The K-arrow logo SVG (329x328 viewBox)
- **GitHub**: The Invertocat SVG (97.6x96 viewBox)

### No new files needed
All SVGs will be inlined directly in the footer JSX, same approach as sql4all.org. This avoids creating separate icon component files for just 4 icons used in one place.

## Verification
1. Run `bun dev` and check `http://localhost:3000`
2. Verify all 4 icons render in the footer below "Built with Suno + ElevenLabs"
3. Verify all links open correct URLs in new tabs
4. Verify icons have proper hover effects
5. Run `bun check` to ensure no lint/format issues
